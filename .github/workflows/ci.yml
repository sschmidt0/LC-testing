name: CI workflow

on: push

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: sschmidt0/LC-testing/.github/workflows/ci.yml@master
      - name: Install
        uses: npm ci
      - name: Tests
        uses: npm run test
